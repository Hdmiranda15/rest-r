import React from 'react';
import './Section.css'; // Importa el estilo del componente

const Section = ({ number }) => (
    <section>
        <h2>Section {number}</h2>
    </section>
);

export default Section;